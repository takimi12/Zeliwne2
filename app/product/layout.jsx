import React from 'react';

const Layout = ({ children }) => {
  return (
    <div>
      {/* Tutaj możesz umieścić elementy wspólne dla wszystkich stron */}
      
        {children}
      

    </div>
  );
};

export default Layout;
